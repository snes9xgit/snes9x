language: cpp

matrix:
  include:
    - os: linux
    - os: osx
    - os: windows

before_script:
  - if [[ "$TRAVIS_OS_NAME" == "windows" ]]; then choco install make; fi

script:
  - make -C libretro
