<!--
  Windows XP compatibility override.

  To build with Windows XP support (requires VS2017 with v141_xp toolset):
    1. Rename this file to Directory.Build.targets
    2. Open the solution in VS2017 and build

  .targets files are imported AFTER vcxproj, so they override the defaults.
-->
<Project>
  <PropertyGroup>
    <PlatformToolset>v141_xp</PlatformToolset>
    <WindowsTargetPlatformVersion>8.1</WindowsTargetPlatformVersion>
  </PropertyGroup>
</Project>
